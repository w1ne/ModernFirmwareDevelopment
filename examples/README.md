# Examples

This directory contains reference implementations for the concepts discussed in the book.

- `ci-cd/`: Example GitHub Actions and GitLab CI pipelines.
- `docker/`: DevContainer and Dockerfile examples for reproducible builds.
- `firmware/`: minimal firmware projects showing folder structure and build setups.
